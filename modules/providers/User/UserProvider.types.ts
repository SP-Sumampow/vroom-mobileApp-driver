// ===========================================================
// Types
// ===========================================================

import {ReactNode} from 'react';

export type UserProviderPropsType = {
  children: ReactNode;
};

export type UserProviderContextType = {
  test: boolean;
};
